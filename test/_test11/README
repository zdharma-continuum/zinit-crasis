Origin:

    https://github.com/Osse/dotfiles/blob/master/.zshrc

revision 280d34a. Added 3 zplugin commands. Normal real-world test with 2 move
operations on commands that are mixture of zplugin commands and "foo && zplugin
..." like commands.
