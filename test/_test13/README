Origin:

    https://github.com/paulirish/dotfiles/blob/master/.zshrc

revision a2a01ed. Added zplugin commands and compinit. The test revealed
incorrect pre_block_end assignment in case when its initial value is 0
(meaning that zplugin is the first command in zshrc).
